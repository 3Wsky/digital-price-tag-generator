# 开发进度记录

更新时间：2026-05-24

## 项目目标

制作一个数码产品展示台价签生成器，主要用于手机门店打印价格标签。输入产品型号后，系统自动从官网获取准确数据（价格、颜色、SKU、服务），从知乎提炼卖点，生成可编辑的价签卡片，最终导出 PDF/PNG 打印。

## 当前项目路径

```text
D:\Codex\2026-05-22\new-chat
```

## 技术栈

- 前端：React 19 + TypeScript + Vite 8
- 后端：Node.js HTTP Server (`server.mjs`)
- 爬虫：Playwright（浏览器渲染级抓取）+ 原生 fetch（静态页解析）
- 数据源：华为商城(官网) + 知乎开放平台(卖点)
- 导出：html2canvas + jsPDF

## 已完成功能

### 编辑器核心
- [x] 页面视图 + 卡片编辑双模式 (`viewMode: 'page' | 'card'`)
- [x] 左栏按视图模式分离功能（页面视图：尺寸/卡片管理；卡片视图：生成/添加元素）
- [x] 多卡片管理（创建、选择、清空内容、填充到、填充全部）
- [x] 统一应用尺寸（自动重命名 + 自动增减空白卡片）
- [x] 自定义卡片命名优先级（`customName` 标记，手动命名 > 产品型号 > 默认命名）
- [x] 元素拖拽、选择、属性编辑
- [x] 10 种元素类型：文字、价格、参数、徽章、二维码、图片、图标卖点、颜色参考、分割线、脚注
- [x] 8 种卖点图标（电池、芯片、护眼、音量、通信、耐摔、快充、影像、整机）
- [x] A4/A3 纸面排版 + 自动装箱算法
- [x] 页面缩放 + 卡片位置调整
- [x] PDF 导出 / PNG 导出 / 打印

### 数据采集与生成
- [x] 知乎开放平台全网搜索代理（5 组问题并行查询）
- [x] 华为商城 Playwright 爬虫（搜索 → 产品页 → SKU/价格/颜色/服务）
- [x] VIVO Shop API 爬虫（价格/SKU/颜色/芯片/电池/屏幕，支持 iQOO 子品牌）
- [x] 双源数据合并（官网为主，知乎提炼卖点）
- [x] 回退机制：官网不可用 → 纯知乎搜索 → 本地通用模板
- [x] `.env` 文件 + dotenv 环境变量加载
- [x] vmall 搜索结果缓存
- [x] VIVO 本地产品缓存（`vivo-products.json`，10个型号，规格不变价格实时刷新，响应 <1s）

### 卡片布局
- [x] 百分比自适应布局（`elementsFromDraft` 根据卡片尺寸等比缩放）
- [x] 最小字号保护（小卡片文字不小于 2.5px）
- [x] 多尺寸验证：100×185、90×130、80×50、105×148

## 已解决问题与关键方法

### 1. 视图模式左栏分离
**问题：** 页面视图和卡片编辑视图显示相同的左栏功能，页面视图不需要生成/编辑功能。
**方法：** 用 `state.viewMode` 条件渲染左栏 `<aside>` 区域。
```tsx
{state.viewMode === 'page' ? (
  <>{/* 尺寸与格式 + 卡片管理 */}</>
) : (
  <>{/* 自动生成 + 添加元素 */}</>
)}
```

### 2. 统一应用尺寸后卡片命名错误
**问题：** 点击"统一应用尺寸"后卡片名称没有按规则递增，需要点两次才生效。
**原因：** React 批量状态更新 + auto-adjust useEffect 竞争。
**方法：** 将尺寸应用、重命名、卡片增减合并为一个原子 `setState` 调用，移除独立的 useEffect。用 `useRef` 读取 input DOM 值避免 React 状态延迟。

### 3. 卡片内容溢出不同尺寸
**问题：** `elementsFromDraft` 使用硬编码 100×185 坐标，切换尺寸后内容错位/截断。
**方法：** 改为百分比布局，所有坐标用 `cardW * pct` / `cardH * pct` 计算：
```typescript
const topPad = cardH * 0.055
const marginX = cardW * 0.1
const contentW = cardW - marginX * 2
```

### 4. 卖点重复（XMAGE 显示两次）
**问题：** 第 3 和第 4 个卖点都使用了 `camera` 变量。
**方法：** 第 4 个卖点检测 camera 是否已在第 3 个卖点中使用，已使用则改用 `'phone'` 图标 + "综合体验更出色"。

### 5. 屏幕尺寸误匹配 "5英寸"
**问题：** 正则 `/[5-9](?:\.\d+)?\s?英寸/` 先匹配到 "6.5英寸" 中的 "5英寸"。
**方法：** 优先匹配小数格式 `/[6-7](?:\.\d+)?\s*英寸/`，排除不现实的 5 英寸：
```typescript
const screenSize = findFirst(scopedText, /\d\.\d\s*英寸/)
  || findFirst(scopedText, /[6-7]\s*英寸/)
  || findFirst(allText, /\d\.\d\s*英寸/) || ''
```

### 6. 颜色名误匹配垃圾数据
**问题：** "精选告白"、"其中橙"、"为钛金"、"李水青" 等被误识别为颜色名。
**方法：** 多层过滤：
- 非颜色词黑名单（`nonColorWords` 正则）
- 正则要求 2-3 字前缀 + 标准颜色后缀
- 回退路径要求完整匹配 `^\s*([一-龥]{2,3}(?:色尾))\s*$`

### 7. 华为商城 SPA 无法直接爬取
**问题：** vmall.com 是纯 SPA，HTTP fetch 获取不到商品数据。
**方法：** 用 Playwright 启动 Chrome 渲染页面：
1. 访问 vmall.com 获取 CSRF cookie
2. 跳转搜索页，拦截 `queryPrd` API 响应获取产品 ID
3. 跳转产品页，从 HTML 正则提取 SKU/服务/规格
4. 用 `page.evaluate()` 从 DOM 提取颜色选项

### 8. 华为商城搜索选错产品变体
**问题：** 搜索 "华为Mate 80 Pro Max" 优先匹配到 "风驰版"。
**方法：** 在产品匹配逻辑中排除变体关键词：
```javascript
if (/风驰|典藏|rs|非凡大师/.test(name)) return false
```

### 9. 官网颜色提取不全
**问题：** SKU 正则提取不到颜色，页面文字提取过多误匹配。
**方法：** 三重提取策略：
1. `page.evaluate()` 查找颜色选择器 DOM 元素
2. `<script>` 标签中 `sbomAbbr` 正则提取
3. 页面文字中 "颜色：" 后的内容解析
三种来源合并去重。

### 10. 知乎搜索 API 未配置
**问题：** `ZHIHU_ACCESS_SECRET` 环境变量未设置，搜索全部回退。
**方法：** 创建 `.env` 文件 + `import 'dotenv/config'` 加载环境变量。

## 当前数据流架构

```
用户输入型号
    │
    ├──→ 官网 API (/api/official/search)
    │       │
    │       ├── 华为 → Playwright 搜索 vmall.com
    │       │         → 获取产品ID → 产品页提取
    │       │         → 返回: 标题、价格、SKU、颜色、Care+、规格
    │       │
    │       ├── VIVO/iQOO → 本地缓存(vivo-products.json) 命中?
    │       │         ├─ 命中 → Shop API 仅刷新价格(~0.8s)
    │       │         └─ 未命中 → Playwright 营销页 + Shop API 全量爬取(~20s) → 写入缓存
    │       │
    │       ├── 荣耀 → Playwright → honor.com
    │       │
    │       ├── 苹果 → fetch SSR → apple.com.cn
    │       │
    │       └── 其他品牌 → 返回 { ok: false }
    │
    ├──→ 知乎 API (/api/zhihu/global-search)
    │       → 5组问题并行搜索
    │       → 返回: 搜索结果文章列表
    │
    └──→ 前端合并 (searchModel)
            │
            ├── 有官网数据 → buildDraftFromOfficial()
            │       官网: 标题/价格/SKU/颜色/服务/规格
            │       知乎: 卖点提炼（芯片/影像/通信/耐摔）
            │
            ├── 无官网但有知乎 → buildDraftFromSearch()
            │       全部从知乎正则提取
            │
            └── 都没有 → buildFallbackDraft()
                    本地通用模板
```

## 各品牌数据源现状

| 品牌 | 官网爬虫 | 知乎兜底 | 数据质量 |
|------|----------|----------|----------|
| 华为 | Playwright → vmall.com | 可用 | 优秀（价格/颜色/SKU/服务全准确） |
| VIVO | Shop API + Playwright 营销页 + 本地缓存 | 可用 | 优秀（10个型号已缓存，价格实时刷新，规格完整） |
| 苹果 | 未实现 | 可用 | 较差（颜色名难提取，SKU无存储规格） |
| 小米 | 未实现 | 可用 | 一般 |
| 荣耀 | 未实现 | 可用 | 一般 |

## 下一步优化方向

### 一、各品牌官网爬虫方案

#### VIVO 官网
- **难点：** vivo 官网（vivo.com.cn）也是 SPA，商品详情页需要渲染
- **方案：** 与华为类似，用 Playwright 搜索 + 提取
  - 搜索：`https://www.vivo.com.cn/search?keyword=xxx`
  - 产品页可能有服务端渲染的 `<script>` 包含初始数据（`__INITIAL_STATE__`）
  - 先尝试 fetch + HTML 解析（快），失败再用 Playwright（慢但稳）
- **备选方案：** vivo 京东/天猫自营旗舰店页面数据相对结构化，可通过京东/天猫搜索 API 获取价格和配置

#### 苹果中国
- **优势：** apple.com.cn 有较好的服务端渲染支持，SEO 友好
- **方案：**
  - 产品页 URL 可预测：`https://www.apple.com.cn/shop/buy-iphone/iphone-17-pro`
  - 页面包含 JSON-LD 结构化数据（`<script type="application/ld+json">`），可直接解析
  - AppleCare+ 价格在页面中有规律结构
  - 颜色名在 `<script>` 标签的配置数据中
  - **推荐先用 fetch + HTML 解析**，不需要 Playwright

#### 小米
- **难点：** mi.com 也是重度 SPA
- **方案：**
  - 小米有开放的商品 API：`https://www.mi.com/product/detail?productId=xxx`
  - 可以先通过小米搜索 API 获取产品 ID
  - 或用 Playwright 与华为方案一致

#### 通用方案（适用于所有品牌）
- **京东搜索：** 京东有结构化搜索 API，商品详情页包含规格参数、价格、SKU
  - 搜索：`https://search.jd.com/Search?keyword=xxx`
  - 详情页：`https://item.jd.com/xxx.html`
  - 京东页面的规格参数表非常完整
- **天猫搜索：** 类似京东
- **优势：** 覆盖所有品牌，数据较结构化
- **劣势：** 价格是零售价非官网价，可能含促销价

### 二、AI 大模型辅助校验

当前硬规则提取的局限：
- 正则无法理解上下文（"5英寸" 是屏幕还是传感器？）
- 新机型需要持续维护规则
- 颜色名种类不断变化

**建议方案：混合模式**
```
硬规则粗提取 → AI 校验修正 → 写入卡片
```
- 硬规则做第一道提取（快、免费、覆盖 80% 场景）
- AI 做第二道校验（检查合理性、修正错误、补充缺失）
- 可调用知乎搜索同一个 API 体系，或用轻量模型
- 输出结构化 JSON，token 消耗很小

### 三、搜索结果缓存与预热

- 当前已有 vmall 内存缓存（`vmallCache` Map）
- 可扩展为文件缓存（JSON），重启不丢失
- 可加入"预热"功能：批量导入常用型号，提前生成数据

### 四、VIVO S50 当前可用方案

VIVO 官网爬虫尚未实现，当前知乎回退数据有以下问题：
- 芯片信息缺失
- 只找到一个颜色（灵感紫）
- 第二个 SKU 标签为"高配版"无价格

**短期改善建议：**
1. 优化知乎搜索查询词，增加 "骁龙" "天玑" "芯片型号" 等关键词
2. 颜色提取增加 VIVO 常见颜色名词库（告白、灵感紫、悠悠蓝、深空黑等）
3. 价格提取增加对京东价格的容错（VIVO S50 官方售价 ¥2999 起）
4. 或：增加京东搜索 API 作为中间层，获取较准确的配置和价格

### 五、其他待优化项
- [ ] 卡片编辑器中元素的缩放/旋转手柄
- [ ] 更多卡片模板（横版桌牌、耳机价签、手表价签）
- [ ] Excel/CSV 批量导入型号
- [ ] 批量导出 PDF
- [ ] 数据来源标注（卡片上显示"数据来自华为商城"等）
- [ ] 复核标记（价格/颜色旁显示"建议复核"标记）

## 常用命令

启动开发服务：
```powershell
cd D:\Codex\2026-05-22\new-chat
$env:ZHIHU_ACCESS_SECRET='你的知乎开放平台密钥'
node server.mjs
```

默认访问：`http://127.0.0.1:5173/`

构建：
```powershell
npm.cmd run build
```

检查：
```powershell
npm.cmd run lint
```

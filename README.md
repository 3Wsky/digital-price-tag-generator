# 数码产品价签编辑器

一个面向手机门店、数码柜台、渠道销售和小团队的在线价签/参数牌生成工具。

输入产品型号后，系统会根据官网数据生成标准化产品价签，并结合知乎 API 搜索到的全网信息做辅助验证，帮助门店快速制作可打印、可导出、可复用的商品展示牌。

## 在线体验

在线体验地址：

```text
https://tag.1go.im
```

如果页面暂时打不开，说明 Cloudflare Pages 自定义域名还在配置或 DNS 生效中。

## 这个项目解决什么痛点？

很多手机门店在做商品展示时，经常遇到这些问题：

- **价签制作慢**：每台手机都要手动查参数、查价格、排版、截图、打印。
- **信息容易错**：型号后缀相似，例如 `Y500`、`Y500i`、`Y500 Pro`，人工复制很容易混淆。
- **格式不统一**：不同员工做出来的价签风格不一致，影响柜台专业度。
- **批量更新麻烦**：新品上市、价格变化、SKU 版本调整后，要反复手动改。
- **设计门槛高**：不是每个门店都有设计人员，也不是每个人都会 PS 或 AI。

本项目希望把这些重复工作自动化：

- 输入型号
- 自动生成产品信息草稿
- 可视化编辑排版
- 一键导出 PNG/PDF
- 直接打印或发给门店使用

## 功能特性

- **型号自动识别**：支持 vivo、iQOO、华为、荣耀等机型。
- **官网数据优先**：价格、SKU、颜色、核心参数以品牌官网/官方商城数据为准。
- **知乎 API 辅助验证**：通过知乎 API 搜索全网相关内容，用于补充卖点和交叉验证信息。
- **静态数据加速**：线上版本优先读取预生成的 `products.json`，访问速度快，不依赖实时后端。
- **可视化编辑**：支持文字、价格、卖点、颜色、规格等模块的拖拽和编辑。
- **多格式导出**：支持导出 PNG、PDF，也支持浏览器打印。
- **批量数据更新**：可通过 GitHub Actions 定时刷新产品数据。
- **适合 Cloudflare Pages 部署**：前端静态托管，部署成本低。

## 数据原则

本项目的数据处理原则：

- **官网优先**：商品标题、SKU、官方价格、颜色、硬件参数以品牌官网/官方商城为准。
- **辅助验证**：知乎 API 搜索结果仅作为全网信息补充和卖点参考。
- **人工可编辑**：生成结果不是最终强制内容，用户可以在页面中手动修正。
- **避免混淆型号**：对相近型号做严格匹配，减少 `Pro`、`i`、`s` 等后缀造成的误用。

## 适用场景

- 手机门店价签制作
- 数码柜台产品参数牌
- 渠道销售物料生成
- 新品卖点整理
- 门店统一展示模板
- 小团队批量制作商品介绍卡

## 技术栈

- React
- TypeScript
- Vite
- Node.js
- Cloudflare Pages
- GitHub Actions
- Zhihu API
- `html2canvas`
- `jspdf`
- `lucide-react`

## 本地运行

安装依赖：

```powershell
npm install
```

启动开发环境：

```powershell
npm run dev
```

构建生产版本：

```powershell
npm run build
```

预览生产构建：

```powershell
npm run preview
```

## 产品数据更新

型号清单在：

```text
data/models.txt
```

手动生成静态产品数据：

```powershell
node server.mjs --build-data
```

生成结果：

```text
public/data/products.json
```

线上部署时，GitHub Actions 会定时运行数据更新任务，并把新的 `products.json` 提交到仓库。

## 部署

推荐部署到 Cloudflare Pages：

- Build command: `npm run build`
- Output directory: `dist`
- Framework preset: `Vite`

详细步骤见：

```text
DEPLOY.md
```

## 定制开发

如果你需要：

- 定制门店价签模板
- 定制手机/家电/数码产品数据源
- 定制 AI 智能体
- 定制企业内部自动化工具
- 定制微信/网页/后台系统

可以联系我。

微信：

```text
xj0991988
```

## License

MIT License

本项目开源供学习、研究和二次开发使用。使用时请自行核验商品信息、价格和宣传内容的准确性。
